# 📘 部署指南：Windows 原生环境部署 **纯文本 Embedding + Reranker 服务**

> 适用于：本地开发、测试、桌面使用、Dify 私有化部署  
> ⚠️ **本版本不支持图像输入，仅处理纯文本**

---

## ✅ 项目架构

- 一个 Python 进程（`main.py`）
- 一个 FastAPI 应用（`app = FastAPI()`）
- 一个 Uvicorn 服务器（监听 `0.0.0.0:8080`）
- 支持 **Embedding** 和 **Reranker** 模型（可选部署）

### 🌐 请求示例

| 请求                                       | 功能                |
| ------------------------------------------ | ------------------- |
| `POST http://localhost:8080/v1/embeddings` | 调用 embedding 模型 |
| `POST http://localhost:8080/v1/rerank`     | 调用 reranker 模型  |

✅ 两者共用：
- 同一个端口（如 8080）
- 同一个进程
- 同一个 GPU 内存（如果使用 CUDA）

✅ 适用于：
- Windows 10/11 + Python + NVIDIA GPU（或 CPU）

---

## 一、前置要求

| 组件        | 要求                      |
| ----------- | ------------------------- |
| 操作系统    | Windows 10/11（64位）     |
| Python      | ≥ 3.10（推荐 3.11）       |
| GPU（可选） | NVIDIA 显卡 + CUDA 12.x   |
| 磁盘空间    | ≥ 10 GB（纯文本模型较小） |

---

## 二、安装依赖

```powershell
# 创建项目目录
mkdir C:\www\text_rag_server
cd C:\www\text_rag_server

# 创建虚拟环境（名称：fastapi_env）
python -m venv fastapi_env

# 启用脚本执行（当前用户）
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# 激活环境
.\fastapi_env\Scripts\Activate.ps1

# 安装 PyTorch（CUDA 12.6 示例）
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126

# 若无 GPU，用 CPU 版：
# pip install torch torchvision torchaudio

# 安装核心依赖
pip install transformers accelerate einops uvicorn fastapi python-dotenv

# 安装 sentence-transformers（可选，部分模型兼容）
pip install sentence-transformers
```

> 💡 注意：`bitsandbytes` 在 Windows 上可能无法用于 4-bit 量化，建议使用 `bf16` 或 `fp16`。

---

## 三、项目结构

```
text_rag_server/
├── fastapi_env/            # 虚拟环境（可自定义）
├── main.py                 # 服务主程序
├── models.py               # 模型加载逻辑（支持纯文本模型）
├── setup.py                # 交互式配置脚本
├── .env                    # 配置文件（由 setup.py 生成）
├── install_service.ps1     # Windows 服务安装脚本
├── uninstall_service.ps1   # windows 服务卸载脚本
└── nssm/                   # NSSM 服务管理器（用于后台运行）
    └── nssm.exe
```

---

## 四、离线下载模型

从 Hugging Face 手动下载以下**纯文本模型**到本地目录（如 `D:\models\`）：

- [Qwen/Qwen3-Embedding-0.6B](https://huggingface.co/Qwen/Qwen3-Embedding-0.6B)
- [Qwen/Qwen3-Reranker-0.6B](https://huggingface.co/Qwen/Qwen3-Reranker-0.6B)
- [BAAI/bge-reranker-v2-m3](https://huggingface.co/BAAI/bge-reranker-v2-m3)（可选）

> ✅ 每个模型目录必须包含：
> - `config.json`
> - `pytorch_model.bin` 或 `model.safetensors`
> - `tokenizer.json` / `tokenizer_config.json`

---

## 五、初始化配置（关键步骤！）

```powershell
# 激活环境
.\fastapi_env\Scripts\Activate.ps1

# 运行初始化脚本
python setup.py
```

💡 此脚本将引导你：
- 选择是否部署 Embedding / Reranker
- 自选模型文件夹路径
- 设置 API_KEY（可选）
- 自动生成 `.env` 文件

---

## 六、启动服务（本地测试）

```powershell
# 方式1：使用 .env 中的端口
python main.py

# 方式2：手动指定端口
uvicorn main:app --host 0.0.0.0 --port 8080
```

---

## 七、验证接口

### 1. 访问 Swagger 文档
> http://localhost:8080/docs

### 2. 测试 Embedding（PowerShell）

```powershell
curl -X POST http://localhost:8080/v1/embeddings `
  -H "Authorization: Bearer sk-qwen" `
  -H "Content-Type: application/json" `
  -d '{ "model": "text-embedding", "input": ["hello world"] }'
```

### 3. 测试 Rerank

```powershell
curl -X POST http://localhost:8080/v1/rerank `
  -H "Authorization: Bearer sk-qwen" `
  -H "Content-Type: application/json" `
  -d '{
    "query": "人工智能是什么？",
    "documents": [
      "人工智能是计算机科学的一个分支。",
      "苹果是一种水果。"
    ]
  }'
```

---

## 八、在 Dify 中配置

进入 **Dify → 知识库 → 创建知识库**

### 1. 配置 Embedding 模型
- **Model Provider**: `OpenAI-API-compatible`
- **Model Type**: `Text Embedding`
- **Model Name**: `Qwen3-Embedding-0.6B`（与部署的模型名一致）
- **API Base URL**: `http://<你的内网IP>:8080/v1`  
  （如 `http://192.168.1.100:8080/v1`）
- **API Key**: `fast8888`（如果修改了配置则与 `.env` 中一致）

### 2. 配置 Reranker 模型
- **Model Provider**: `OpenAI-API-compatible`
- **Model Type**: `Rerank`
- **Model Name**: `Qwen3-Reranker-0.6B`
- 其他同上

> ⚠️ 注意：Docker 容器默认无法通过 `localhost` 访问宿主机。  
> **解决方案**：
> - 在 Windows 防火墙中放行 8080 端口
> - 使用宿主机真实 IP（如 `192.168.x.x`）
> - 或在 `docker-compose.yaml` 中添加：
>   ```yaml
>   extra_hosts:
>     - "host.docker.internal:host-gateway"
>   ```
>   然后在 Dify 中填：`http://host.docker.internal:8080/v1`

---

## 九、安装为 Windows 自启动服务（生产环境）

> 适用于：后台长期运行、开机自启

### 1. 准备
- 确保已运行 `python setup.py` 生成 `.env`
- 下载 [nssm](https://nssm.cc/download)，解压到项目目录

### 2. 安装服务（以管理员身份运行 PowerShell）

```powershell
# 首次运行需设置执行策略
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser

# 安装服务（端口 8081）
.\install_service.ps1 -ServiceName "TextRAG-Service" -Port 8081
```

### 3. 服务管理

```powershell
# 启动
net start TextRAG-Service

# 停止
net stop TextRAG-Service

# 查看日志
type logs\stdout.log
```

### 4. 卸载服务

```powershell
.\uninstall_service.ps1 -ServiceName "TextRAG-Service"
# 并手动在 wf.msc 中删除防火墙规则
```

---

## 十、性能优化建议

| 问题       | 解决方案                                                     |
| ---------- | ------------------------------------------------------------ |
| 显存不足   | 使用 `torch_dtype=torch.float16`（而非 bfloat16）            |
| 启动慢     | 首次加载模型需 1–5 分钟，属正常现象                          |
| CPU 占用高 | 限制线程：`torch.set_num_threads(4)`（可在 `main.py` 中添加） |
| 无法访问   | 临时关闭 Windows Defender 防火墙测试                         |

---

## 📌 总结

通过 **FastAPI + Transformers**，你可以在 Windows 原生环境部署高性能的 **纯文本 Embedding 和 Reranker 服务**，并以 OpenAI 兼容 API 形式供 Dify 调用。

- ✅ 无需 WSL
- ✅ 无需图像处理依赖（PIL、base64 等已移除）
- ✅ 支持 Qwen、BGE 等主流开源模型
- ✅ 可作为 Windows 服务长期运行

> 💡 **最佳实践**：  
> 在新机器部署时，只需复制整个项目目录 → 运行 `setup.py` → 执行 `install_service.ps1`，即可完成全自动配置！

---

📘 **进阶提示**：未来若需支持多模态，可基于此架构新建分支，避免污染纯文本主干。